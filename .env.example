# fenora — Environment Variables
# Copy this to .env and fill in your values
# For Render/Railway: add these in the dashboard env vars section

# ── Flask ──────────────────────────────────────────────────────
SECRET_KEY=change_this_to_a_long_random_string_in_production
FLASK_DEBUG=false

# ── Database ───────────────────────────────────────────────────
# Local:
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wardrobe_db
DB_USER=postgres
DB_PASSWORD=wardrobe123

# Production (Render/Railway provides this automatically):
# DATABASE_URL=postgresql://user:password@host:port/dbname

# ── Email (Gmail SMTP) ─────────────────────────────────────────
# Setup: Enable 2FA → go to myaccount.google.com/apppasswords → generate
EMAIL_SENDER=cloudberryyoh@gmail.com
EMAIL_PASSWORD=abcd hpej iubw qqnx kcix
# ── Frontend URL (for email links) ─────────────────────────────
FRONTEND_URL=https://your-app.vercel.app

# ── Optional: Gemini AI for chatbot ────────────────────────────
GEMINI_API_KEY=AIzaSyDNARsZNQxEWInjWzBaelPKwcQhU41e9OM
